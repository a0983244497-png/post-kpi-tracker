# PostgreSQL 連線字串（Railway 會自動注入 DATABASE_URL）
DATABASE_URL=postgresql://user:password@localhost:5432/post_kpi

# 團隊共用密碼（瀏覽器打開網站時的 Basic Auth 密碼）
TEAM_PASSWORD=your_password_here

# 不需手動設定，Railway 自動提供
# PORT=3000
